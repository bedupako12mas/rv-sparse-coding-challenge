This is where I will keep my study notes for this entire project:

:3

first understanding:

int rows -> number of rows of A

int cols -> number of columns of A

const double* A -> pointer to the matrix A (row major format) (read-only)

const double* x -> pointer to the vector to be multiplied (read-only)

int* out_nnz -> pointer to some list of integers idk??? 

double* values -> pointer to list of non zero values in A or output?

int* col_indices -> pointer to list of column indices of nz-elements  

int* row_indices -> pointer to list of column indices of nz-elements

double* y -> pointer to the output vector y  

Requirement:

convert A into CSR format (into caller provided buffers)

compute the product A*x = y using the CSR representation

write the output into the caller provided buffer (double* y) 

fun fact 

provided command in the doc doesnt work:

>>> gcc -lm -o run challenge.c

my  compiler version:  

gcc (Ubuntu 15.2.0-4ubuntu4) 15.2.0
Copyright (C) 2025 Free Software Foundation, Inc.
This is free software; see the source for copying conditions.  There is NO
warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.

The command doesnt work due to the linker processing from left to right and trying to find themain() function in the binary. 

not necessarily for all: 

Looking through the testing code in main to figure out:

int out_nnz -> number of non zero elements in CSR format

First iteration: Stage 2 code looks correct. Probably an issue with stage 1

Cannot be floating point errors since the error is too large in most cases

the expression is cols*row + col

All tests pass
